
  <div class="container">
    <div class="row my-4">
      <div class="col-md-3 mt-3 mx-auto">
        <h2>ログイン</h2>
        <%= form_with model: @user, url: user_session_path, local: true do |f| %>

         <table class="table table-borderless table-sm mt-3">
           <tr>
             <th>E-メール</th>
           </tr>
           <tr>
             <td>
              <div class="field">
               <%= f.email_field :email, autofocus: true, autocomplete: "email", size: 30 %>
              </div>
             </td>
           </tr>
           <tr>
             <th>パスワード</th>
           </tr>
           <tr>
             <td>
              <div class="field">
               <%= f.password_field :password, autocomplete: "current-password", size: 30 %>
              </div>
             </td>
           </tr>
           <tr>
             <td>
              <% if devise_mapping.rememberable? %>
               <div class="field">
                <%= f.check_box :remember_me %>
                <%= f.label :remember_me %>
               </div>
              <% end %>
             </td>
           </tr>
           <tr>
             <td>
              <div class="actions">
               <%= f.submit "ログイン", class: "btn btn-info btn-block" %>
              </div>
             </td>
           </tr>
           <% end %>
         </table>
      </div>
    </div>
    <div class="row">
      <div class="col-md-5 mx-auto">
        <p>会員登録がまだの方は
         　<%= link_to "新規登録", new_registration_path(resource_name), class: "btn btn-primary" %>
         　からお願いします!
         </p>
         <p>
           パスワードをお忘れの方は<%= link_to "こちら", new_password_path(resource_name) %>
         </p>
      </div>
    </div>
  </div>


